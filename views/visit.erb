<h2>Записатьcя на прием</h2>
<p>Заполните форму и мы свяжемся с вами в ближайшее время.</p>

<script type="text/javascript">
  $(function(){ // Обертка позволяет выполнить функцию порсле полной загрузки страницы
    $('#color').simplecolorpicker(); // Инициализация плагина simplecolorpicker JQuery
    $('#datetimepicker').datetimepicker(); // Инициализация плагина jquery-datetimepicker
  });
</script>

<form action="/visit" method="POST" role="form">
  <div class="mb-3">
    <label class="form-label">Ваше имя:</label>
    <input name="client[name]" type="text" class="form-control" placeholder="Введите ваше имя" value="">
  </div>

  <div class="mb-3">
    <label class="form-label">Ваш телефон:</label>
    <input name="client[phone]" type="text" class="form-control" placeholder="Введите ваш телефон" value="">
  </div>

  <div class="mb-3">
    <label class="form-label">Время записи:</label>
    <input id="datetimepicker" name="client[datestamp]" type="text" class="form-control" placeholder="Введите дату и время" value="">
  </div>

  <!-- <div class="mb-3">
  <label class="form-label">Выберете барбера:</label>
  <select name="barber" class="form-select">
    <% @barbers.each do |barber| %>
      <option <%= @barber == barber.name ? "selected" : "" %>><%= barber.name%></option>
    <% end %>
  </select>
  </div> -->

  <div class="mb-3">
  <label class="form-label">Выберете цвет:</label>
    <select id="color" name="client[color]" class="form-select">
      <option value="#7bd148">Green</option>
      <option value="#5484ed">Bold blue</option>
      <option value="#a4bdfc">Blue</option>
      <option value="#46d6db">Turquoise</option>
      <option value="#7ae7bf">Light green</option>
      <option value="#51b749">Bold green</option>
      <option value="#fbd75b">Yellow</option>
      <option value="#ffb878">Orange</option>
      <option value="#ff887c">Red</option>
      <option value="#dc2127">Bold red</option>
      <option value="#dbadff">Purple</option>
      <option value="#e1e1e1">Gray</option>
    </select>
  </div>

  <button type="submit" class="btn btn-primary">Записаться</button>
</form>
